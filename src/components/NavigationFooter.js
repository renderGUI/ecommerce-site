import classes from "./NavigationFooter.module.scss";

const NavigationFooter = () => {
  return (
    <footer className={classes.container}>
      <p>Made by renderGUI | 2022</p>
    </footer>
  );
};

export default NavigationFooter;
